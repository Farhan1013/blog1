<div class= "container">
	<%= form_for(@post, :url => {:action => :genesis_post, :controller => :posts} ) do |f| %>

	<div class="field">
		<%=f.text_field :title, :class => 'form-control', :placeholder => 'Put your Title here' %>


	<div class="field">	
		<%=f.text_area :body, :class => 'form-control', :placeholder => 'Whats on your Mind?' %>
	</div>
	<br>
	<br>
	<br>
	<br>
	<div class="actions">
		<%=f.submit  :class => 'btn btn-primary' %>
	</div>
	</div>

	<% end %>
</div>
