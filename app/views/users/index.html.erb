<div class="heading">
	<p>ChitterChatterr</p>
</div>


 <% if current_user %>
            <%= link_to "Log Out", logout_path %>
            <ul><%= link_to 'Search for comments', comment_search_path %></ul>
            <% else %>

            <div class= "buttons">
	            <%= link_to "Sign Up", signup_path, :class => "btn btn-default btn" %> or
	            <%= link_to "Log In", login_path, :class => "btn btn-primary btn" %>
            </div>
            </ul>

            <% end %>